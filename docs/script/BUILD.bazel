filegroup(
    name = "script",
    srcs = [
        ":js-kernel",
        ":nb",
    ],
    visibility = ["//:__pkg__"],
)

genrule(
    name = "nb",
    srcs = ["nb.ts"],
    outs = ["nb.js"],
    cmd = "$(ESBUILD) $< > $@",
    toolchains = ["//third_party/esbuild:resolved"],
)

genrule(
    name = "js-kernel",
    srcs = ["js-kernel.ts"],
    outs = ["js-kernel.js"],
    cmd = "$(ESBUILD) $< > $@",
    toolchains = ["//third_party/esbuild:resolved"],
)
