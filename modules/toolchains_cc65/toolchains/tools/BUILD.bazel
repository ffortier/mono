load("@rules_shell//shell:sh_binary.bzl", "sh_binary")

package(default_visibility = ["//visibility:public"])

# TODO: Needs a wrapper for windows as well
sh_binary(
    name = "cc65_wrapper",
    srcs = ["cc65_wrapper.sh"],
)
